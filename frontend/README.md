# Frontend

Dependency-free, responsive tablet UI for the Class 2 Plants demo. It is served at `/` by the local backend, and calls only `/api/process-lesson`.

The microphone and listen controls use browser facilities solely as desktop-demo fallbacks; neither is a deployment claim for offline Santali speech support.
