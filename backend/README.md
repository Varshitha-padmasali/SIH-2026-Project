# Backend

FastAPI service for the local Smart Education MVP. It exposes the lesson pack and joins the existing speech/translation module with child-friendly content mapping.

Run from the repository root:

```bash
PYTHONPATH=backend uvicorn app.main:app --reload
```
